project.name=storybot
buildsupport.dir=${basedir}/buildsupport
project.dir=${basedir}
config.flexunit.additional=${buildsupport.dir}/config/flexunit-compile-config.xml
build.number.default=155

#keystore.path.desktop=${project.dir}/certs
#keystore.name.desktop=STORYBOT.p12
#keystore.password.desktop=password